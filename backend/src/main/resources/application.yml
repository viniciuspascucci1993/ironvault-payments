spring:
  application:
    name: payments-backend

  datasource:
    url: jdbc:postgresql://localhost:5490/ironvault
    username: ironvault
    password: ironvault

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org.flywaydb: INFO

server:
  port: 8080